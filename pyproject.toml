[project]
name = "workshop"
version = "0.0.1"
description = "Micro services + micro frontend workspace"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "services/*",
    "libs/python",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
