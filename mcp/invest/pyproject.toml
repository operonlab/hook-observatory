[project]
name = "invest-mcp"
version = "0.1.0"
description = "Invest MCP adapter — portfolio, positions, trades over Core API"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
