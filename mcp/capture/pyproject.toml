[project]
name = "capture-mcp"
version = "0.1.0"
description = "Capture MCP server — progressive data enrichment pipeline"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
