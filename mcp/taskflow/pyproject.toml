[project]
name = "taskflow-mcp"
version = "0.1.0"
description = "Taskflow MCP adapter — tasks, transitions, progress over Core API"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
