---
source_hash: f9715c9f
source_lang: en
target_lang: en
translated_at: 2026-02-24
---

# Workshop Web

Single React application with domain modules.

## Run
pnpm install && pnpm dev

## Structure
- src/shell/ — App shell (layout, navigation, auth)
- src/modules/ — Domain modules (lazy-loaded)
- src/plugins/ — Plugin UI runtime
- src/shared/ — Shared components and constants
- src/stores/ — Global state (Zustand)
- src/api/ — API client
Hook execution for SessionEnd: 2 hooks executed successfully, total duration: 3666ms
Hook execution for SessionEnd: 2 hooks executed successfully, total duration: 3784ms
