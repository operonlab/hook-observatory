host: "0.0.0.0"
port: 7860

models:
  florence2:
    model_id: "microsoft/Florence-2-large"
    dtype: "float16"
    auto_load: false
  sam_hq:
    model_id: "placeholder"
    auto_load: false

idle_timeout: 300   # seconds before unloading idle models
unload_interval: 60 # seconds between idle-check sweeps
max_image_size: 2048 # resize images with max dim larger than this
