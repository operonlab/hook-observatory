[project]
name = "workshop-fleet"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "uuid-utils>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
