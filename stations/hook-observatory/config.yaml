env_prefix: "HOOK_OBS"

port: 10100
host: "127.0.0.1"
database_url: "postgresql+asyncpg://joneshong:REDACTED@localhost/workshop"
secret_key: "change-me-in-production"
session_cookie_name: "workshop_session"
session_max_age: 604800  # 7 days

# Spool settings (flat keys: spool_*)
spool_dir: "~/.hook-observatory/spool"
spool_drain_interval: 2.0
spool_batch_size: 100
