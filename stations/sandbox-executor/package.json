{
  "name": "@workshop/sandbox-executor",
  "version": "1.0.0",
  "private": true,
  "description": "MCP Server: Execute code in sandbox — 1 tool call replaces N Bash/MCP calls",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "catalog:"
  }
}
