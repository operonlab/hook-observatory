[project]
name = "gpu-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "torchvision",
    "transformers",
    "accelerate",
    "fastapi",
    "uvicorn[standard]",
    "pillow",
    "numpy",
    "pyyaml",
]
