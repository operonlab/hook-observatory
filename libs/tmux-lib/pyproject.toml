[project]
name = "workshop-tmux-lib"
version = "0.1.0"
description = "Workshop tmux session management primitives"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tmux_lib"]
