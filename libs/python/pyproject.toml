[project]
name = "workshop-lib"
version = "0.0.1"
description = "Shared Python library for workshop services"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
