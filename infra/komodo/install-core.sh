#!/bin/bash
# DEPRECATED — Use docker-compose.yml instead:
#   cd infra/komodo && docker compose up -d
#
# This script is kept for reference only.
# See README.md for full deployment instructions.
echo "Use 'docker compose up -d' instead. See README.md."
exit 1
